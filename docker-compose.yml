version: "3.8"

services:
  wpff-front:
    image: nginx:alpine
    container_name: wpff_front_nginx
    restart: unless-stopped
    expose:
      - "80"
    volumes:
      - /var/web_root/html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - web

networks:
  web:
    external: true
